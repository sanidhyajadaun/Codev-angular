import { AllowtextonlyDirective } from './allowtextonly.directive';

describe('AllowtextonlyDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowtextonlyDirective();
    expect(directive).toBeTruthy();
  });
});
