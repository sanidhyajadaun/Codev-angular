import { OtpfocusDirective } from './otpfocus.directive';

describe('OtpfocusDirective', () => {
  it('should create an instance', () => {
    const directive = new OtpfocusDirective();
    expect(directive).toBeTruthy();
  });
});
